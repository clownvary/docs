/**
 * this is a temporary file used only by eslint.
 * it is not included in the main gulp build and will
 * be removed once eslint is integrated into the
 * build pipeline.
 *
 * do not delete or modify this file!
 */
const config = {
  resolve: {
    extensions: ['.js', '.jsx'],
    modules: ['src', 'examples', 'node_modules']
  }
};

module.exports = config;
