const rootUrl = window.__environment__.ROOT_URL;

export const permits = `${rootUrl}/rest/permit/permits`;

export const centers = `${rootUrl}/rest/permit/centers`;

// export const status = rootUrl + '/permit.do?method=loadStatus';

export const savePermitFilters = `${rootUrl}/rest/permit/permitfilters`;

export const permitExtraInfo = `${rootUrl}/rest/permit/extrainfo`;

export const permitStatusList = `${rootUrl}/rest/permit/permitstatus`;

export const permitAccessible = `${rootUrl}/rest/permit/accessible`;

export const permitEventInfo = `${rootUrl}/rest/permit/permitinfo`;

export const saveCreatedByMe = `${rootUrl}/rest/permit/filters/createdbyme`;
