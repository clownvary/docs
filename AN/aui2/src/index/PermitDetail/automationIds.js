const PREFIX = 'permitdetail-';

const filters = {
  type: `${PREFIX}filter-type`,
  customer: `${PREFIX}filter-customer`,
  organization: `${PREFIX}filter-organization`,
  agents: `${PREFIX}filter-agents`
};

export default {
  filters
};
