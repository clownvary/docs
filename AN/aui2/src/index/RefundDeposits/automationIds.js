const PREFIX = 'deposit-';

const deposits = {
  fee: `${PREFIX}deposits-fee`
};

const extraFees = {
  feeDescription: `${PREFIX}extraFees-feeDescription`,
  amount: `${PREFIX}extraFees-amount`
};

const rentalFees = {
  fee: `${PREFIX}rental-fee`
};

export default {
  deposits,
  extraFees,
  rentalFees
};
