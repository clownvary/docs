import createFieldLayout from './createFieldLayout'

const FlexFormFieldLayout = createFieldLayout({
  sm: [3, 6, 3],
  align: 'center',
  className: 'form__group--flex',
})

export default FlexFormFieldLayout
