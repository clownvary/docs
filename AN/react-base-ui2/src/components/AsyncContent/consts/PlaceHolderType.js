
export const TEXT = 'text';
export const ICON = 'icon';


/**
 * Enum that determines the waiting state.
 * @enum {string}
 */
const PlaceHolderType = {
  /** Indicates the waiting state with pure text */
  TEXT: 'text',
  /** Indicates the waiting state with both text and icon */
  ICON: 'icon'
};

export default PlaceHolderType;
