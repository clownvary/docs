import { defineMessages } from 'react-intl';

const PREFIX = 'app.dev.master.component.footer';

export default defineMessages({
  logoTitle: {
    id: `${PREFIX}.logoTitle`,
    defaultMessage: 'Configuration of site'
  }
});
