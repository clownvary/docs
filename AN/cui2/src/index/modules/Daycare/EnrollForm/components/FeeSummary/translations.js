import { defineMessages } from 'react-intl';

const PREFIX = 'app.modules.daycare.EnrollForm.FeeSummary';

export default defineMessages({
  title: {
    id: `${PREFIX}.title`,
    defaultMessage: 'Fee Summary'
  },
  addToCart: {
    id: `${PREFIX}.addToCart`,
    defaultMessage: 'Add to cart'
  },
  total: {
    id: `${PREFIX}.total`,
    defaultMessage: 'Total'
  }
});
