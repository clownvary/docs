import { defineMessages } from 'react-intl';

const PREFIX = 'app.modules.daycare.Program.GeneralInfo';

export default defineMessages({
  allAges: {
    id: `${PREFIX}.allAges`,
    defaultMessage: 'All Ages'
  }
});
