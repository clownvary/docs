import { defineMessages } from 'react-intl';

const PREFIX = 'app.modules.daycare.Program.ExtraDetail';

export default defineMessages({
  moreInformation: {
    id: `${PREFIX}.moreInformation`,
    defaultMessage: 'More Information'
  },
  imageAltText: {
    id: `${PREFIX}.imageAltText`,
    defaultMessage: 'This image cannot be displayed.'
  }
});
