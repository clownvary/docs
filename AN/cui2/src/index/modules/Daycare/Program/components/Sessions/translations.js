import { defineMessages } from 'react-intl';

const PREFIX = 'app.modules.daycare.Program.Sessions';

export default defineMessages({
  session: {
    id: `${PREFIX}.session`,
    defaultMessage: 'Session'
  },
  sessions: {
    id: `${PREFIX}.sessions`,
    defaultMessage: 'Sessions'
  }
});
