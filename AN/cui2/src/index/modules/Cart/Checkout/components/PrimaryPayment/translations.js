import { defineMessages } from 'react-intl';

const PREFIX = 'app.modules.cart.Checkout.PrimaryPayment';

export default defineMessages({
  title: {
    id: `${PREFIX}.title`,
    defaultMessage: 'Payment Method'
  }
});
